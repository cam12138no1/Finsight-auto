# ================================================================
# Finsight Auto — Environment Variables
# For Render deployment (PostgreSQL + Python Worker)
# ================================================================

# Render PostgreSQL Database (required)
# Get from: Render Dashboard > Your Database > Connect > External Connection String
DATABASE_URL="postgresql://user:password@host:5432/finsight_auto_db"

# OpenRouter API for AI Analysis (required)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY="sk-or-v1-..."

# NextAuth Configuration (required)
# Generate secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-here"
NEXTAUTH_URL="https://your-app.onrender.com"

# JWT Configuration (required)
# Generate secret: openssl rand -base64 32
JWT_SECRET="your-jwt-secret-here"

# Vercel Blob Storage (optional — falls back to PostgreSQL if not set)
# Only needed if deploying to Vercel instead of Render
# BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# Python Worker URL (for Render internal service communication)
# If Worker and Web are on the same Render account, use internal URL
WORKER_API_URL="http://localhost:8000"

# Application
NEXT_PUBLIC_APP_NAME="FinSight Auto"
NEXT_PUBLIC_APP_VERSION="2.0.0"
